import React from 'react'; 

const Title=({title})=> {
    return(
        <div className="section-title"> 
        <h4 id ="top"> 
            {title}
        </h4>
        <div> </div>

        </div>

    );
}
export default Title;